# Build artifacts
/kiro-extract
/kiro-extract.exe
/dist/

# Local config — may contain auth_token cookie
/.env

# Extracted credentials — never commit
/kiro-out/
*-credentials.json
*-credentials.csv
*refresh-tokens.txt
*token-refresh.txt
all-credentials.json
auth.json

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
